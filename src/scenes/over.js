import Scene from '../libs/scene';

export default class OverScene extends Scene {
    constructor(...args) {
        super(...args);
    }
    init() {}
    preload() {}
    pause() {}
    create() {}
    update() {}
}
