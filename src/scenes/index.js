import LoadingScene from './loading';
import PlayingScene from './playing';
import OverScene from './over';

export default {
    loading: LoadingScene,
    playing: PlayingScene,
    over: OverScene,
};
